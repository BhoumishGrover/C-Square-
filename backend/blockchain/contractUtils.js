// blockchain/contractUtils.js
export const mintCredit = async (companyId, amount) => {
  // Connect to smart contract
  // Mint "amount" of tokens to the company
  console.log(`Minting ${amount} credits to ${companyId}`)
}